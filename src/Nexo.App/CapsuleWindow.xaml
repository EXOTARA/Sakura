<Window x:Class="Nexo.App.CapsuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:a11y="clr-namespace:Nexo.App.Views.Controls"
        AutomationProperties.Name="Aviso de Sakura"
        Width="418"
        Height="136"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="True"
        Topmost="True">
    <!--
        2026-09-14 — el margen es el sitio de la sombra y del temblor de la burbuja (BubbleMotion).
        Antes el borde medía lo mismo que la ventana y la sombra se cortaba en recto; D40 ya lo había
        resuelto así en los controles rápidos.
    -->
    <Border x:Name="CapsuleBorder"
            Margin="24"
            Padding="14,12"
            CornerRadius="22"
            Background="{DynamicResource BrushSurfaceRaised}"
            BorderBrush="{DynamicResource BrushBorder}"
            BorderThickness="1"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              Direction="270"
                              ShadowDepth="8"
                              Opacity="0.38"
                              Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border x:Name="StatusBadge"
                    Width="34"
                    Height="34"
                    CornerRadius="17"
                    Background="{DynamicResource BrushAccentSoft}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left">
                <Grid>
                    <a11y:DecorativeMark Focusable="True" x:Name="BrandFlowerIcon"
                                    Width="19"
                                    Height="19"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource BrushAccent}"
                                    Style="{StaticResource SakuraFlowerMarkStyle}" />
                    <Border Width="13"
                            Height="13"
                            Margin="0,0,1,1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Background="{DynamicResource BrushBackground}"
                            CornerRadius="7">
                        <a11y:DecorativeText x:Name="StatusIcon"
                                   Text="•"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource BrushAccent}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                           Text="Sakura"
                           FontSize="{DynamicResource FontSizeBody}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushTextPrimary}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="DetailText"
                           Margin="0,3,0,0"
                           Text="Listo"
                           FontSize="{DynamicResource FontSizeFootnote}"
                           Foreground="{DynamicResource BrushTextSecondary}"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="285" />
            </StackPanel>
        </Grid>
    </Border>
</Window>

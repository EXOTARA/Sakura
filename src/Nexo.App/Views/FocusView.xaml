<UserControl x:Class="Nexo.App.Views.FocusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Temporizadores y sesiones privadas en tu equipo."
                           Style="{StaticResource MutedTextStyle}" />
            </StackPanel>
            <Border Grid.Column="1"
                    Padding="8,4"
                    Background="{DynamicResource BrushAccentSoft}"
                    CornerRadius="9"
                    VerticalAlignment="Top">
                <TextBlock x:Name="TimerStateText"
                           Text="LISTO"
                           FontSize="{DynamicResource FontSizeCaption}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushTextSecondary}" />
            </Border>
        </Grid>

        <Border Grid.Row="1"
                Style="{StaticResource SubtleCardStyle}"
                Margin="0,0,0,12">
            <StackPanel>
                <Border x:Name="AssociatedTaskBadge"
                        Margin="0,0,0,8"
                        Padding="9,5"
                        HorizontalAlignment="Center"
                        Background="{DynamicResource BrushAccentSoft}"
                        BorderBrush="{DynamicResource BrushAccentBorder}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Visibility="Collapsed">
                    <TextBlock x:Name="AssociatedTaskText"
                               Text=""
                               FontSize="{DynamicResource FontSizeCaption}"
                               Foreground="{DynamicResource BrushTextSecondary}" />
                </Border>
                <TextBlock x:Name="TimerLabelText"
                           Text="Sin sesión activa"
                           HorizontalAlignment="Center"
                           FontSize="{DynamicResource FontSizeCallout}"
                           Foreground="{DynamicResource BrushTextSecondary}" />
                <TextBlock x:Name="RemainingTimeText"
                           Text="00:00"
                           Margin="0,5,0,7"
                           HorizontalAlignment="Center"
                           FontSize="{DynamicResource FontSizeDisplay}"
                           FontWeight="SemiBold" />
                <ProgressBar x:Name="TimerProgressBar"
                             Height="7"
                             Minimum="0"
                             Maximum="100"
                             Value="0"
                             Foreground="{DynamicResource BrushAccent}"
                             Background="{DynamicResource BrushBorder}" />
                <TextBlock x:Name="TimerDetailText"
                           Margin="0,8,0,0"
                           Text="Elige una duración para comenzar."
                           TextAlignment="Center"
                           Style="{StaticResource MutedTextStyle}" />
                <UniformGrid Margin="0,12,0,0" Columns="4">
                    <Button x:Name="PauseButton"
                            Content="Pausar"
                            Margin="0,0,3,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="Pausar sesión de enfoque"
                            Click="PauseButton_Click" />
                    <Button x:Name="ResumeButton"
                            Content="Continuar"
                            Margin="3,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="Continuar sesión de enfoque"
                            Click="ResumeButton_Click" />
                    <Button x:Name="FinishButton"
                            Content="Finalizar"
                            Margin="3,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            ToolTip="Termina ahora y cuenta el tiempo transcurrido"
                            AutomationProperties.Name="Finalizar sesión de enfoque"
                            AutomationProperties.HelpText="Termina la sesión ahora mismo y registra el tiempo ya transcurrido. Distinto de Cancelar, que descarta la sesión sin guardarla."
                            Click="FinishButton_Click" />
                    <Button x:Name="CancelButton"
                            Content="Cancelar"
                            Margin="3,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            ToolTip="Descarta la sesión sin registrarla"
                            AutomationProperties.Name="Cancelar sesión de enfoque"
                            Click="CancelButton_Click" />
                </UniformGrid>

                <!--
                    Diseño D3.1 — aviso no modal de fin de sesión. Aparece para toda finalización
                    real (natural o manual, con tarea asociada o sin ella): nunca para Cancelar,
                    que descarta sin dejar rastro (ver FocusManager.Cancel/Finish).
                -->
                <Border x:Name="SessionCompletionNoticeBorder"
                        Margin="0,12,0,0"
                        Padding="14"
                        Background="{DynamicResource BrushSurfaceRaised}"
                        BorderBrush="{DynamicResource BrushBorder}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource RadiusMedium}"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="SessionCompletionTitleText"
                                   Text=""
                                   FontWeight="SemiBold"
                                   FontSize="{DynamicResource FontSizeCallout}" />
                        <TextBlock x:Name="SessionCompletionDetailText"
                                   Margin="0,4,0,0"
                                   Text=""
                                   TextWrapping="Wrap"
                                   Style="{StaticResource MutedTextStyle}" />
                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cerrar"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="11,6"
                                    AutomationProperties.Name="Cerrar aviso de sesión completada"
                                    Click="DismissSessionCompletionButton_Click" />
                            <Button x:Name="StartAnotherSessionButton"
                                    Content="Seguir 10 min más"
                                    Margin="7,0,0,0"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="11,6"
                                    AutomationProperties.Name="Seguir diez minutos más"
                                    Click="StartAnotherSessionButton_Click" />
                            <Button x:Name="CompleteAssociatedTaskButton"
                                    Content="Marcar como completada"
                                    Margin="7,0,0,0"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="11,6"
                                    Visibility="Collapsed"
                                    AutomationProperties.Name="Marcar tarea asociada como completada"
                                    Click="CompleteAssociatedTaskButton_Click" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource SubtleCardStyle}">
                <StackPanel>
                    <TextBlock Text="Hoy" Style="{StaticResource MutedTextStyle}" />
                    <TextBlock x:Name="TodaySessionsText"
                               Text="0 sesiones"
                               Margin="0,4,0,0"
                               FontSize="{DynamicResource FontSizeTitle2}"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource SubtleCardStyle}">
                <StackPanel>
                    <TextBlock Text="Tiempo enfocado" Style="{StaticResource MutedTextStyle}" />
                    <TextBlock x:Name="TodayMinutesText"
                               Text="0 min"
                               Margin="0,4,0,0"
                               FontSize="{DynamicResource FontSizeTitle2}"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock x:Name="TopTaskText"
                           Margin="0,0,0,10"
                           Text=""
                           TextWrapping="Wrap"
                           Visibility="Collapsed"
                           Style="{StaticResource MutedTextStyle}" />

                <TextBlock Text="Actividad reciente"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold" />
                <ItemsControl x:Name="RecentSessionsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,6"
                                    Padding="9,7"
                                    Style="{StaticResource SubtleCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TitleText}"
                                                   FontSize="{DynamicResource FontSizeFootnote}"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding SubtitleText}"
                                                   Margin="0,2,0,0"
                                                   Style="{StaticResource MutedTextStyle}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DurationText}"
                                               Margin="10,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="{DynamicResource FontSizeFootnote}"
                                               FontWeight="SemiBold" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="RecentSessionsEmptyText"
                           Text="Aún no hay sesiones de enfoque registradas."
                           Margin="0,0,0,4"
                           Style="{StaticResource MutedTextStyle}" />

                <!--
                    2026-09-16 — enfoque flexible. Empezar es lo difícil: «solo 5 min» baja la barrera,
                    y la última duración usada queda a un toque. Ya no hay tipos de sesión
                    (enfoque, descanso, temporizador): un rato es un rato.
                -->
                <TextBlock Text="¿Cuánto rato?"
                           Margin="0,16,0,8"
                           FontWeight="SemiBold" />
                <UniformGrid Columns="2">
                    <Button Content="Solo 5 min para empezar"
                            Tag="Focus:5"
                            Margin="0,0,5,5"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="PresetButton_Click" />
                    <Button Content="15 min"
                            Tag="Focus:15"
                            Margin="5,0,0,5"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="PresetButton_Click" />
                    <Button Content="25 min"
                            Tag="Focus:25"
                            Margin="0,5,5,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="PresetButton_Click" />
                    <Button x:Name="LastDurationButton"
                            Content="La última · 40 min"
                            Tag="Focus:40"
                            Margin="5,5,0,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="PresetButton_Click" />
                </UniformGrid>

                <TextBlock Text="Otra duración (minutos)"
                           Margin="0,16,0,8"
                           FontWeight="SemiBold" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="92" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="CustomMinutesTextBox"
                             AutomationProperties.Name="Minutos de enfoque"
                             Text="40"
                             Style="{StaticResource CompactTextBoxStyle}"
                             ToolTip="Minutos" />
                    <Button Grid.Column="1"
                            Content="Iniciar"
                            Margin="8,0,0,0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="StartCustomButton_Click" />
                </Grid>
                <TextBlock x:Name="ActionStatusText"
                           Margin="0,9,0,0"
                           Text=""
                           TextWrapping="Wrap"
                           Style="{StaticResource MutedTextStyle}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
